[project]
name = "bharatresilio"
version = "0.1.0"
description = "BharatResilio OpenEnv Environment"
dependencies = [
    "openenv-core",
    "fastapi",
    "uvicorn"
]

[project.scripts]
server = "server.app:app"

[tool.openenv]
env_module = "env:BharatResilioEnv"